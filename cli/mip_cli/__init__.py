"""Official CLI for the Memory Intelligence Platform — a thin wrapper over
`mip_sdk`. No business logic lives here: every command parses args, calls the
SDK, and formats the result.
"""

from __future__ import annotations

__version__ = "0.1.0"
